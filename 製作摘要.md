# 焊接各部位元件：路線記憶小遊戲製作摘要

## 目的與範圍

本小遊戲用於任務階段「焊接各部位元件」。玩家需記住系統短暫示範的一條焊接路線，並以游標、觸碰或手把虛擬游標重走；完成後才可推進該任務階段。

本摘要只定義第一類「路線記憶焊接」玩法；先前討論的焊點順序記憶玩法可作為後續關卡，尚未納入本次實作範圍。

## 已確認的玩法規則

1. 畫面中央是一個焊接面板，常駐顯示由正式參考圖還原的 **五條青綠色虛線基礎線路**。
2. 每條路線由多個短線段組成。線段會在分支處分開、之後可重新匯合，也可以有共用線段或重疊關係。
3. 每一局重新開始時，系統都要隨機選出一組正確的起點到終點路徑；玩家每次都必須重新記憶，不能背固定答案。
4. 路網必須可保證本局有且只有一個有效解答；錯誤支線不可意外回接成第二個可通關路徑。
5. 系統先完整預覽正解一次，再交給玩家操作。
6. 玩家只能由正確起點開始，並以小線段為單位、依正確連接順序前進。走錯分支、跳到不相連線段、或違反順序即失敗。
7. 成功：通過此焊接關卡，回傳任務完成事件／進度。
8. 失敗：立即離開小遊戲，並損耗 1 個指定 Item 耗材；不要因玩家畫得慢而失敗。

## 三層視覺狀態

### 1. 常駐路網

- 三條候選路線始終保留，使用青綠色虛線。
- 虛線路網是玩家辨識可走路段與分支的依據，**不可在預覽後 Fade Out**。
- 路線設計避免圓形節點、端點圓圈、橋接符號或裝飾性跨線圖示。
- 分支與平行支線需保留足夠間距，至少約三個虛線線寬，讓玩家能看清是不同支線。

### 2. 系統預覽焊接特效

- 系統以一個沿正解移動的焊點／焊槍示範走法。
- 焊點持續噴出火花；身後依序形成粉紅至珊瑚紅的高熱熔融焊痕，核心可偏白熱、外緣帶暖色光暈。
- 預覽走完後，只有這一層高熱焊接效果 Fade Out；常駐虛線路網保持可見。

### 3. 玩家焊接痕跡

- 玩家每通過一段正確線段，播放同類焊槍、火花與高熱焊接效果。
- 高熱光暈可以隨時間自然冷卻、淡出；已通過的正確路段要留下較低亮度、穩定可見的已焊接痕跡。
- 玩家移動速度慢時，先前高熱特效消退屬正常狀態，不影響已完成線段或判定。

## 建議的資料與判定方式

將路網實作為圖（graph），不要以自由手繪像素比對作為核心判定：

- `segments`：短線段資料，包含 `id`、兩端座標、可接續的鄰接線段與顯示路徑。
- `routes`：程式先把基礎線路的交點拆成圖節點；每局在每個分岔點重新抽選，組成新的 `segmentId[]`，不是只從固定完整路線中抽一條。
- `activeRoute`：本局隨機抽出的唯一正解。
- `playerIndex`：玩家目前已正確完成到 `activeRoute` 的第幾段。
- `weldState`：`preview`、`playing`、`success`、`failed`、`closed`。

建議流程：

1. 開啟時抽選可通關且唯一解的 `activeRoute`。
2. `preview`：沿 `activeRoute` 逐段播放焊點位置、火花與高熱焊痕。
3. 預覽結束後等待約 1 秒，再淡出預覽高熱層並切至 `playing`。
4. 玩家命中下一個正確線段時，保留永久焊痕並把高熱特效移到該段。
5. 玩家命中錯誤／非相鄰／過序線段時，切至 `failed`，關閉面板並扣除一個耗材。
6. `playerIndex` 完成最後一段時，切至 `success`，再送出與任務階段掛接的成功事件。

### 輸入判定建議

- 指標與觸控：將輸入座標投影到目前可接續的線段；落在線段容許寬度內才算選中。不要要求沿曲線逐像素描畫。
- 手把：右搖桿移動科技銲槍游標；靠近正確起點後按住 RT 即吸附在線上。焊接期間只要大致把右搖桿推向分岔方向，焊點就會貼線前進，不要求精準描線。
- 若採連續拖曳，線段完成後應鎖定為已焊接，並只接受其相鄰的下一正確線段。
- 以圖資料驗證起點、相鄰性、順序與唯一解；不要以玩家移動時間作為失敗條件。

## UI 與手把注意事項

本小遊戲為瀏覽器互動 UI，必須套用專案 UI 技能：[`.codex/skills/browser-ui-focus-visuals/SKILL.md`](.codex/skills/browser-ui-focus-visuals/SKILL.md)。本次沒有刻意例外。

- 隱藏面板內瀏覽器原生的 `:focus`／`:focus-visible` 外框、背景與陰影；保留語意、焦點能力與 ARIA。
- 視覺選取狀態由遊戲自訂 class／attribute 呈現，不可把原生焦點框當成遊戲選取效果。
- 游標、觸控、手把虛擬游標與方向操作應以最後使用的輸入方式為準，避免多種選取視覺同時搶控制權。
- 若面板提供「重試」、「離開」等可點擊按鈕，開啟時要建立預設可選目標；左搖桿可循環切換可用按鈕，A 鍵須呼叫與指標點擊相同的 handler，且採按壓邊緣偵測避免重複觸發。

## 視覺設計已確認／避免事項

### 保留

- 深藍／藍灰科幻修復面板。
- 青綠虛線候選路網。
- 粉紅、珊瑚紅、白熱核心的熔融焊接視覺。
- 橘色火花與移動焊點。
- 短線段、分支、匯合、可辨識的共用線段。

### 避免

- 每個轉折點加上圓形節點或圓環。
- 讓兩條平行分支貼得太近、難以辨識。
- 橋接符號、跨線裝飾符號，或將非連通交叉誤畫成可選節點。
- 把常駐青綠虛線在預覽後隱藏。
- 讓玩家因焊接移動太慢而失敗。
- 以概念圖的路線幾何當作正式遊戲資料；待使用者提供新的參考圖後，再確定最終路網美術與布局。

## 整合與驗證清單

1. 先找出「焊接各部位元件」對應的任務、stage ID、互動點與耗材 Item ID；這些 ID 目前尚未在本摘要中確認。
2. 將成功事件接到該 objective／stage；失敗才扣耗材，成功不可扣除。
3. 每局抽選後靜態驗證：起點可達終點、正解唯一、每段均相鄰、沒有無法離開的必要段。
4. 測試預覽層淡出後青綠虛線仍在，且玩家完成痕跡會保留。
5. 測試滑鼠長按、觸控長按、手把右搖桿與 RT；離開按鈕仍支援左搖桿選取、A 鍵單次觸發與 B 鍵返回。
6. 測試錯誤分支、跳段、失敗提示、成功退出與重新開啟後重抽路線；本版尚未真正扣除耗材。
7. 測試快速與慢速玩家操作；慢速只會讓高熱效果冷卻，不得改變成功判定。
8. 除單元／流程測試外，需在實際瀏覽器 Preview 驗證輸入與特效。

## 目前狀態（2026-08-19 第一階段）

- 已完成：正式路網圖資料、每個交錯分岔重新抽選、金屬板彈窗、科技銲槍游標、熱點／火花／焦痕、滑鼠與觸控長按、手把右搖桿與 RT 吸附、成功／失敗退出，以及 `Game 1`～`Game 3` Debug 快捷指令。
- 已預留：成功後交由第二階段接管的 callback；目前回傳 `false`，因此第一階段成功後直接退出。
- 暫未完成：失敗時真正扣除「金屬碎片」、正式任務 Stage／Objective 掛接與第二階段小遊戲。
- 驗證：焊接路線與 Debug 指令測試 7 項通過；連同今天的任務／對話修復測試共 55 項通過，正式建置成功。
