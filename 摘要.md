# Echoes 專案摘要

> 整理範圍：2026-07-28 ～ 2026-07-29 目前為止  
> 專案位置：`D:\Codex\Echoes`  
> GitHub：<https://github.com/sbaak85/Echoes>  
> GitHub Pages：<https://sbaak85.github.io/Echoes/>

## 1. 專案方向

《Echoes Beyond the Stars》目前是一個以場景圖片為底圖的 2D／斜視角探索遊戲原型。

目前主要方向：

- 使用八方向角色圖片在場景上移動。
- 場景移動由 NavMesh 限制可行走區域，Collision 定義障礙物。
- 同時支援鍵盤、滑鼠、觸控與手把操作。
- 使用獨立的 Windows 場景編輯器製作 NavMesh、Collision 與玩家出生點。
- 遊戲與編輯器共用同一份場景 JSON，避免顯示、尋路與碰撞資料不一致。
- 未來會將多張場景圖片無縫拼成一張可連續移動的大地圖。

## 2. 目前主要成品

### 遊戲前端

- 主要程式：`app/movement-lab.tsx`
- 主要樣式：`app/globals.css`
- 測試場景：`public/maps/map_test01.png`
- 場景資料：`public/maps/map_test01.scene.json`
- 本機啟動：雙擊根目錄的 `啟動遊戲.bat`
- Pages 靜態版：由 `github-pages/` 與 `vite.pages.config.ts` 建置。

### 場景編輯器

- 可執行檔：根目錄 `MapEditor.exe`
- 完整原始碼：`tools/MapEditor/`
- 格式說明：`docs/scene-format.md`
- 技術：C#、Windows Forms、.NET 6 Windows。

## 3. 遊戲目前具備的操作與能力

### 3.1 八方向角色移動

角色使用以下八張方向圖：

- `01_N_Back.png`
- `02_NE_BackRight.png`
- `03_E_Right.png`
- `04_SE_FrontRight.png`
- `05_S_Front.png`
- `06_SW_FrontLeft.png`
- `07_W_Left.png`
- `08_NW_BackLeft.png`

方向判斷加入約 18 度的正方向容許範圍，因此接近水平或垂直的輸入會採用 N／E／S／W 圖片，不會永遠只顯示斜方向。

人物腳下原本的假陰影已關閉。

### 3.2 鍵盤、滑鼠與觸控

- WASD／方向鍵：直接移動角色。
- 點擊場景：自動尋路前往目標位置。
- 單次點擊：顯示倒三角形插入效果與圓環動畫。
- 按住滑鼠或持續觸控：每 0.12 秒、且目標移動超過約 10 世界座標單位時，才重新指定路徑，避免過度消耗效能。
- 持續按住時不會反覆播放圓環；改為讓倒三角形跟隨游標，讓重新輸入看不出跳動。
- 一般遊玩時，實體滑鼠游標也使用圓形十字游標，不顯示 A／X 字樣。

### 3.3 手把操作

- 左搖桿：直接控制角色移動。
- 右搖桿：移動遊戲內的虛擬圓形十字游標。
- A 鍵或 X 鍵：對游標位置下達移動指令。
- 互動物件架構已預留；未來點擊互動物件時可走到互動位置並觸發事件。
- 右搖桿啟用虛擬游標時會隱藏系統游標，避免畫面同時出現兩個游標。

手把有兩層輸入來源：

1. 瀏覽器原生 `navigator.getGamepads()`。
2. 本機 Windows 手把橋接程式 `scripts/gamepad-bridge.ps1`，網址為 `http://127.0.0.1:3001/state`。

本機雙擊 `啟動遊戲.bat` 時會一起啟動手把橋接。橋接只在 `localhost`／`127.0.0.1` 使用；GitHub Pages 不會使用這個 Windows 橋接，只能依靠瀏覽器原生 Gamepad API。

### 3.4 自動尋路、NavMesh 與 Collision

- 點擊目標後會在 NavMesh 上計算可行走路徑。
- 角色不能穿越場景 Collision。
- Debug 可顯示玩家碰撞範圍與場景 Collision。
- 碰到障礙物時加入沿表面切線滑動的處理，減少只有一點接觸就完全卡死的情況。
- Debug 的「碰撞滑動寬容度」預設為 55%，範圍為 20%～100%。
- 寬容度是滑動輔助強度，不是單純縮小 Collision；仍應優先確保場景 Collision 的輪廓正確。

### 3.5 音訊

腳步聲：

- 來源：`Assets/Audio/芝生の上を歩く.mp3`
- 網頁檔：`public/audio/grass-footsteps.mp3`
- 角色移動才播放，停止移動立即暫停。
- 移動速度 210 時，以 1.7 倍播放速度為基準，再依實際移動速度增減。

BGM：

1. `異星長夜 (1).mp3`
2. `異星長夜.mp3`

- 兩首依序播完後回到第一首循環。
- 預設開啟，預設音量 35%。
- Debug 選單有 BGM ON／OFF 與音量拉桿。
- 若瀏覽器阻擋自動播放，第一次點擊或按鍵時會再次嘗試播放。
- 分頁隱藏時暫停，回到遊戲時繼續嘗試播放。

## 4. Debug 選單目前功能

- 顯示／隱藏玩家 Collision。
- 顯示／隱藏場景 Collision／NavMesh 相關資料。
- 顯示手把連線與輸入診斷。
- 調整角色移動速度。
- 調整角色顯示尺寸。
- 調整碰撞滑動寬容度。
- BGM ON／OFF。
- BGM 音量。

Debug 面板已限制最大高度並可垂直捲動，避免新增功能後超出畫面。

## 5. MapEditor 第一版功能

### 5.1 圖片與視圖

- 可從 Windows 選擇 PNG、JPG、JPEG、WebP、BMP、GIF、TIFF 等常見格式。
- 顯示細格線與水平／垂直尺規。
- 格線可顯示／隱藏，可調整格距與開關吸附。
- 支援放大、縮小、符合視窗。
- 滑鼠中鍵或按住 Space 拖曳可平移視圖。

### 5.2 場景工具

- 選取、移動與重新命名圖形。
- 手動繪製 NavMesh 多邊形。
- 手動繪製 Collision 多邊形。
- 快速建立矩形 Collision。
- 快速建立圓形 Collision。
- 拖曳整個圖形、頂點或圓形半徑。
- 放大／縮小選取圖形。
- 設定玩家出生點與八方向朝向。
- Undo／Redo。
- 儲存場景 JSON，或匯出圖片與 JSON 到遊戲的 `public/maps`。

繪製多邊形時，可用雙擊、右鍵或 Enter 完成；Escape 取消。

### 5.3 已完成的 Node 增減功能

此功能適用於 NavMesh 多邊形及 `polygon` 類型 Collision；矩形與圓形不開放增減 Node，以免破壞它們原本的幾何型態。

刪除 Node：

1. 切換到「選取」工具。
2. 點擊一個多邊形。
3. 點擊黃色 Node；選中後會變成藍色。
4. 按鍵盤 Del，或在 Node 上按右鍵選「刪除 Node」。
5. 也可使用右側「刪除 Node」按鈕。

刪除後多邊形會自動重新封閉。多邊形至少保留 3 個 Node，因此三角形不能再刪點。

新增 Node：

- 選取多邊形後，在邊線附近按右鍵，選「新增 Node」，會在游標最接近的邊線位置插入新點。
- 先選取一個 Node，再按右側「插入 Node」，會在該 Node 與下一個 Node 的中間新增一點。

注意：若沒有選取 Node，按 Del 代表刪除整個選取圖形。右側另有明確的「刪除圖形」按鈕。

### 5.4 MapEditor 穩定性處理

- 拖曳時以 16ms Timer 合併重繪要求，避免每一筆滑鼠訊息都直接重畫。
- 拖曳時降低圖片縮放品質，放開後恢復高品質。
- 加入滑鼠 Capture 遺失與中斷編輯復原。
- 加入原生繪圖錯誤攔截及 `MapEditor.log` 診斷。
- 加入應用程式層級例外攔截，錯誤時取消本次拖曳並嘗試回復場景。
- 已通過 25,000 次 Node 拖曳壓力測試。
- 已通過 Node 插入、刪除及最低 3 點限制測試。
- 已通過隱藏啟動／關閉 UI smoke test。

修改 `tools/MapEditor/` 原始碼後，必須重新 Publish，並用新版本覆蓋根目錄 `MapEditor.exe`；只修改 C# 原始碼不會自動更新 EXE。

## 6. 場景 JSON 架構

目前格式版本為 v1，主要欄位：

- `image`、`world`：圖片檔名與世界尺寸。
- `grid`：格線大小、顯示與吸附設定。
- `playerSpawn`：出生位置與八方向朝向。
- `navMesh`：一個或多個可行走多邊形。
- `collisions`：polygon、rectangle、circle 障礙物。
- `interactables`：已預留互動物件位置、點選半徑、互動落點、啟動距離與 action。
- `worldLayout`：已預留場景在大型世界中的相對位置。
- `connections`：已預留出口／入口範圍、目標場景、落點、朝向及場景相對位置。

目前遊戲固定載入 `map_test01.scene.json`。尚未加入正式場景切換與多場景拼接預覽。

互動物件到達後，執行期會送出 `echoes:interaction` 瀏覽器事件；`map_test01` 目前還沒有正式啟用的互動物件。

## 7. 本機版與 GitHub Pages 的差異

GitHub Pages 可以執行：

- 鍵盤、滑鼠、觸控移動。
- 點擊自動尋路。
- NavMesh、Collision、八方向角色與 Debug。
- 腳步聲、BGM 及音量控制。
- 瀏覽器原生支援的手把。

GitHub Pages 不能執行：

- `MapEditor.exe`。
- `啟動遊戲.bat`／PowerShell 自動準備流程。
- Windows 本機手把橋接。
- 直接把編輯結果寫回 GitHub 專案。

若未來希望網頁也能編輯場景，需要另外製作 Web 版 MapEditor，並用下載 JSON、File System Access API 或 GitHub API 處理儲存。

## 8. 本機啟動與準備方式

直接雙擊：

`D:\Codex\Echoes\啟動遊戲.bat`

啟動器會：

1. 建立 `.runtime` 記錄資料夾。
2. 嘗試啟動 Windows 手把橋接。
3. 檢查 `http://localhost:3000/` 是否已經運行。
4. 優先尋找 Codex 內附的 Node.js，否則使用系統 Node.js。
5. 若缺少 `node_modules`，首次雙擊會自動執行 `pnpm install --frozen-lockfile`。
6. 啟動本機開發伺服器並開啟瀏覽器。

主要記錄檔：

- `.runtime/game-server.log`
- `.runtime/game-server-error.log`
- `.runtime/gamepad-bridge.log`
- `.runtime/gamepad-bridge-error.log`

## 9. 建置與檢查指令

遊戲前端：

```powershell
pnpm run build
pnpm run build:pages
pnpm run lint
```

MapEditor：

```powershell
dotnet build tools\MapEditor\MapEditor.csproj -c Release
dotnet publish tools\MapEditor\MapEditor.csproj -c Release -r win-x64 --self-contained false -o tools\MapEditor\publish
```

Publish 完成後，將 `tools\MapEditor\publish\MapEditor.exe` 複製到專案根目錄。此版本是 framework-dependent，目標電腦需有相容的 .NET 6 Windows Desktop Runtime。

建置暫存資料夾 `tools/MapEditor/bin/`、`obj/`、`publish/` 已加入 `.gitignore`；根目錄的正式 `MapEditor.exe` 則要保留並提交。

## 10. 重要注意事項

- 多邊形資料只儲存各 Node，不需要重複儲存第一點；繪圖與碰撞會自動封閉最後一點到第一點。
- NavMesh 與 Collision 多邊形至少需要 3 點。
- 不要把矩形／圓形直接改成任意點數，除非同步將其 `shape` 轉成 `polygon`。
- 場景圖片尺寸與 JSON 的 `image/world` 尺寸必須一致。
- 角色座標以腳底位置為基準，不是圖片中心。
- BGM 與腳步聲在 `Assets/Audio` 保留原始中文檔名，在 `public/audio` 另放網頁使用的英文檔名；這是刻意的雙份來源。
- 瀏覽器有音訊自動播放限制，不能保證完全無互動就播放有聲音的 BGM。
- GitHub Pages 是靜態網站，Windows EXE、PowerShell 與本機橋接不會在 Pages 上運行。
- 目前 Pages 與本機遊戲都讀取同一份場景資料設計；新增場景時應延續此原則。
- 修改核心移動、Collision 或 Node 拖曳後，應重新執行建置、自我測試與拖曳壓力測試。

## 11. 未來已確認的方向

優先順序大致如下：

1. 持續擴充 MapEditor，但保持第一版操作簡單。
2. 加入場景出口／入口範圍編輯。
3. 設定目標場景、進入後落點與朝向。
4. 設定各場景在世界中的相對位置。
5. 提供多張圖片拼接預覽。
6. 遊戲執行期支援多場景無縫連續移動。
7. 正式加入互動物件及 A／X／滑鼠點擊互動。

## 12. Git 狀態（建立本摘要時）

- 分支：`main`
- 遠端：`origin` → `https://github.com/sbaak85/Echoes`
- 已推送提交：`a3aa6d0b4998a4b970c33359ed2a8cf7ab1e23b8`
- 提交名稱：`Add scene editor and expand game controls`
- 該提交已包含遊戲修改、音訊、場景 JSON、手把橋接、`MapEditor.exe` 與完整編輯器原始碼。
- 本 `摘要.md` 是在上述提交完成後新增；若要讓另一台電腦直接從 GitHub 取得，仍需再 Commit／Push 本檔。

## 13. 給下一個 Codex 的接手提示

開始修改前應先閱讀：

1. 本 `摘要.md`
2. `docs/scene-format.md`
3. `tools/MapEditor/README.md`
4. `app/movement-lab.tsx`
5. `tools/MapEditor/EditorCanvas.cs`

請維持以下原則：

- 遊戲與 MapEditor 使用相同 JSON 結構。
- 所有輸入方式最後應進入相同的移動／互動指令架構。
- 連續輸入要節流，不要在每次滑鼠事件重算昂貴路徑。
- 編輯器拖曳要合併重繪並保留例外復原，避免再次出現閃退。
- 新功能先保留資料架構，再逐步開放 UI，不要一次把場景編輯器做得過度複雜。
