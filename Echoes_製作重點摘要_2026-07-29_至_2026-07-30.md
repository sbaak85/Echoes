# Echoes 製作重點摘要（2026-07-29～2026-07-30）

> 專案：Echoes Beyond the Stars  
> 專案路徑：`D:\Codex\Echoes`  
> GitHub：`https://github.com/sbaak85/Echoes`  
> GitHub Pages：`https://sbaak85.github.io/Echoes/`

## 一、遊戲啟動與輸入操作

- 本機啟動流程已調整為首次雙擊時自動檢查並完成必要準備，降低換電腦或重新 Pull 後無法啟動的問題。
- 完成滑鼠／觸控點擊移動：點擊場景座標後，角色會自動尋路並依移動方向套用八方向面向。
- 支援按住滑鼠或持續觸控來連續更新目的地；持續輸入時不會反覆播放完整的點擊圓環動畫，改由游標上方的倒三角形提示跟隨。
- 八方向判定加入正四方向容許角度，接近水平或垂直時會正確使用 N／S／E／W 圖片。
- 一般滑鼠游標與手把虛擬游標統一為圓形十字樣式，移除 A／X 字樣，避免同頁出現兩個游標。
- 手把支援：
  - 左搖桿直接控制角色移動。
  - 右搖桿控制遊戲內虛擬游標。
  - A／X 可指派移動或執行互動。
  - START 開啟／關閉 DEBUG 選單。
  - DEBUG 開啟時，十字鍵上下循環選項、左右調整拉桿、A 確認、B 關閉；左搖桿仍可移動角色。
  - 加入 Windows 原生手把橋接方案，處理瀏覽器 Gamepad API 無法正常取得手把輸入的情況。

## 二、角色移動、碰撞與音效

- 關閉角色腳下原本的假陰影。
- 改善角色與場景 Collision 接觸時容易卡死的問題，加入沿碰撞表面滑動的處理。
- DEBUG 選單加入碰撞滑動寬容度調整，方便現場測試不同場景碰撞手感。
- 移動時循環播放草地腳步聲，停止移動時立即停止；播放速度會隨移動速度改變。
- 腳步聲基準：角色移動速度 `210` 時，以 `1.7×` 音檔播放速度為基礎。
- 遊戲開始後循環播放兩首「異星長夜」BGM，DEBUG 選單提供 BGM ON／OFF 與音量拉桿。

## 三、MapEditor 第一階段功能

- 場景編輯器正式整理至 `MapEditor/`，執行檔為 `MapEditor/MapEditor.exe`。
- 支援載入常見圖片格式，並以尺規、細格線、縮放與向量工具協助鋪設場景資料。
- 已具備手動 NavMesh、多邊形 Collision、玩家出生點與面向等基礎場景資料編輯能力。
- 多邊形完成封閉後仍可編修 Node：
  - 選取 Node 後可用 Delete 或右鍵刪除。
  - 在線段最近位置按右鍵可新增 Node。
  - 刪除或新增後，多邊形會自動維持封閉。
- 針對長時間拖曳 Node 容易閃退的情況，已改善拖曳期間的繪製與狀態處理穩定性。
- 編輯器與遊戲共用 `public/maps` 場景 JSON；編輯器儲存後，遊戲與 DEBUG 顯示可讀取同一份 NavMesh、Collision、互動區及引導線資料。

## 四、互動多邊形與對話系統

- 新增亮黃色「互動多邊形」：不參與物理阻擋，只負責偵測角色或游標接觸並觸發互動。
- 目前第一種互動類型為「對話」，右側設定區可調整互動動詞，並以「更多…」開啟對話腳本編輯器。
- 新增「互動 Point」：可放在互動多邊形範圍內或邊線上，並設定八方向面向；互動時角色會走近該點並轉向指定方向。
- 沒有互動 Point 時，不做額外互動尋路檢查，直接採用一般點擊移動前往游標指定座標，避免不必要的卡頓。
- 互動提示 UI：
  - 角色接觸互動區時，提示顯示在角色腳下。
  - 游標接觸互動區時，提示顯示在游標上方。
  - 兩種提示互斥，後發生的接觸會取代先前提示。
  - 鍵盤模式顯示對應互動鍵；游標模式改用簡單滑鼠向量圖示，左鍵區塊以青色表示。
- 對話視窗已縮小至畫面下方中央，背景約 30% 透明，右下角有呼吸動畫的向下三角提示。
- 對話開啟時，點擊畫面任意位置都能顯示下一頁／下一句，不必點在視窗內；鍵盤空白鍵及互動鍵也可繼續對話。
- 對話視窗外的游標改成「書頁＋右側省略號」向量圖示，移入對話框內則使用手指游標。
- 對話文字由左至右逐字顯示，所有既有對話與預設速度統一為 `0.02 秒／字`。
- 連續標點符號會視為一個顯示單位，例如 `...` 只計一次刷字延遲；`... ...` 則視為兩個單位。
- 對話腳本編輯器：
  - 發話者改為下拉選單。
  - 預設角色為 `Sbaak`、`Echo`，並可透過「＋新增」擴充角色。
  - 新句未選發話者時，會延續上一句發話者，直到重新指定其他角色。
  - 每句可跨頁顯示，但句子邊界不因畫面分頁而改變。

## 五、強制引導線

- MapEditor 新增雙向箭頭「強制引導線」，可設定生效寬度，用於斜坡、樓梯、長梯等需要限制角色移動角度的場景。
- 角色進入生效範圍後，輸入方向會投影至引導線切線方向，使角色沿場景斜度移動。
- 已處理互動 Point 與引導線端點過近時可能發生的來回拉扯；抵達互動 Point 後會取消該次引導。
- 最新修正將引導線定位為「只修正實際位移角度」：
  - 不取代點擊尋路的原始目的地。
  - 自動移動進入引導線後會選定適合的出口方向。
  - 抵達引導線端點時，從當前位置重新規劃一次前往原目的地的路徑。
  - 暫時忽略剛離開的同一條引導線，直到角色離開其生效範圍，避免被再次吸回、卡住或來回位移。

## 六、場景格式與目前資料

- `public/maps/map_test01.scene.json` 已擴充：
  - `interactables`：互動多邊形、類型、動詞、互動 Point、面向及對話腳本。
  - `dialogue.speakers`：可選發話者清單。
  - `dialogue.characterDelaySeconds`：逐字顯示速度。
  - `movementGuides`：引導線節點與生效寬度。
- 目前測試場景資料：
  - NavMesh：3 組。
  - Collision：9 組。
  - 互動多邊形：7 組。
  - 強制引導線：2 組。
  - 7 組互動對話的刷字速度皆為 `0.02 秒／字`。
- 場景出口／入口、跨場景落點與朝向、場景相對位置及多張圖片無縫拼接架構仍保留為後續擴充方向。

## 七、角色行走素材狀態

- 已將 32 幀等距行走參考圖隔幀挑選成 16 幀，並裁切為角色置中的單張圖片，存放於 `public/characters/walk/`。
- 已嘗試以主角造型轉換東南方向行走姿勢，並建立原參考角色與生成角色重疊的姿態檢查流程。
- 目前第 2 幀仍有左右腳判斷錯誤，相關主角行走素材製作已依要求暫停，不應直接視為可用正式素材。

## 八、測試與建置結果

- 最新強制引導線尋路修正完成後，前端正式建置已通過。
- 本階段曾完成前端一般建置、GitHub Pages 建置及 MapEditor Release 建置。
- `MapEditor/MapEditor.exe` 已重新發佈，檔案時間為 2026-07-30 02:19。
- 尚需以實際遊玩繼續確認：反向點擊穿越引導線、引導線與互動 Point 相鄰、障礙旁引導出口等組合情況。

## 九、Git 狀態與注意事項

- 目前分支：`main`，追蹤 `origin/main`。
- 2026-07-29 已有兩筆主要提交：
  - `a3aa6d0`：Add scene editor and expand game controls。
  - `d5d3827`：調整地圖編輯器。
- 7/29 提交後至目前的互動系統、對話系統、強制引導線及相關 UI／場景資料仍存在尚未提交的工作區異動。
- 未追蹤內容包含對話編輯器來源、每日摘要、UI 預覽與行走素材；Commit 前應逐項確認，不要把未完成角色素材或暫存預覽誤當成正式遊戲資源。
- `MapEditor/MapEditor.exe` 必須與 C# 原始碼同步發佈；僅修改原始碼但未替換 EXE，使用者啟動時仍會得到舊功能。

## 十、建議下一步

1. 實機測試最新引導線反向目的地修正，確認不再卡住且能繼續前往原點擊座標。
2. 測試互動 Point、引導線、Collision 三者相鄰時的優先順序與停止條件。
3. 在 MapEditor 長時間拖曳 Node、插入／刪除 Node 並反覆存檔，確認穩定性。
4. 確認工作區異動後分批 Commit，將程式功能、場景 JSON、文件與未完成角色素材分開處理。
5. 後續再擴充其他互動類型，以及多場景出口／入口與無縫地圖拼接預覽。
