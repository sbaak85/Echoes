# 2026-08-27 第三章章末存檔與第四章銜接摘要

## 摘要定位

- 本機專案權威路徑：`C:\Users\sbaak.fang\ChatGPT\EchoesBeyond`。
- 本次工作承接 `chapter03-End` 黑幕白字幕與既有可攜式 SaveData 系統，完成第三章結尾至第四章的正式存檔關卡。
- 本次包含阻擋式確認視窗、Options 存檔介面、鍵盤／滑鼠／手把與虛擬游標操作；後續修改與測試必須繼續遵守 [瀏覽器 UI 與手把操作規範](../../.codex/skills/browser-ui-focus-visuals/SKILL.md)。本次沒有刻意例外。

## 1. `chapter03-End` 黑幕存檔關卡

- `chapter03-End` 維持原設定：黑幕淡入 `0.5 秒`、白字幕停留 `4 秒`、正常淡出 `2 秒`。
- ChapterFlow 的黑幕字幕 action 新增可選的 `beforeFadeOutCheckpointId`；只有 `chapter03-End` 掛上 `chapter04-entry-save`。
- 白字幕停留結束後會先隱藏字幕並保留全黑畫面，等待存檔 checkpoint 完成；存檔成功前不會開始黑幕 FadeOut。
- 其他黑幕字幕沒有 checkpoint，不受此次特殊章末流程影響。

## 2. 第四章存檔確認視窗

- 黑幕上新增第四章 SAVE CHECKPOINT 確認視窗，詢問：`要手動儲存目前的遊戲進度嗎?`
- `確認`：關閉章末提示並打開 Options 的「存檔」頁籤，讓玩家選擇手動存檔欄位。
- `自動儲存`：粉紅色主文字，直接覆寫 `autosave`，不再顯示第二次確認。
- 手動存檔期間若玩家直接關閉 Options、尚未完成寫檔，會回到章末存檔確認視窗；黑幕仍保持全黑。
- 自動或手動寫檔失敗時不會切換章節，也不會點亮黑幕；視窗會保留錯誤訊息供玩家重試。
- 寫檔成功是唯一放行條件；存檔清單重新整理即使失敗，也不會把已成功寫入的存檔誤判為失敗。

## 3. 第四章資料與讀檔一致性

- 新增 `app/chapter04-transition.ts`，集中管理：
  - 章節編號：`4`
  - 章節 ID：`chapter04`
  - 顯示名稱：`第四章`
  - 進入旗標：`chapter04-entered`
- 章末存檔寫入前即建立第四章快照，內容包含：
  - `currentChapter: 4`
  - `chapter04-entered: true`
  - `story-subtitle:chapter03-End:1` 已完成 ID
- 存檔摘要的章節名稱與 ID 固定寫成 `第四章`／`chapter04`，不會被仍停留在第三章的任務資料覆蓋。
- 實際執行中的章節只在 SaveData 寫入成功後才更新為第四章，並同步通知 BGM 與劇情接觸條件；接著由原本 `chapter03-End` 的 `2 秒` FadeOut 點亮遊戲畫面。

## 4. 輸入與阻擋規則

- 存檔確認視窗會阻擋世界點擊、角色移動、生存時間推進、任務 Debug 快捷與劇情 Skip。
- 鍵盤方向鍵可切換兩個選項，Enter／Space 確認。
- 手把十字鍵或左搖桿左右切換選項，A 鍵啟動最後取得操作權的項目。
- 虛擬游標在手把模式下仍會顯示；右搖桿取得操作權後，A 鍵點擊游標所在按鈕。方向輸入重新取得操作權後，游標不會干涉目前選項。
- 原生瀏覽器 focus 框已隱藏，使用專案自訂高亮顯示目前選項。

## 5. 驗證結果

- `pnpm run build`：通過。
- 新增／更新的章末存檔、SaveData、Options／手把與電力分配相容測試：`33 / 34` 通過；唯一失敗是既有的 `chapter03-section-9`／`chapter03-final` 深度相等測試仍把兩段刻意不同的 Line ID 視為錯誤。
- 完整 `test:items` 等價測試：`231 / 239` 通過、`8` 項失敗。
- 本次新增的測試均通過：
  - 黑幕在 checkpoint Promise 完成前不會 FadeOut。
  - 第四章快照含章節編號、完成事件與 `chapter04-entered`。
  - 手動／自動兩條存檔入口與手把操作已接線。
  - 章末確認視窗套用自訂 focus 視覺並保留虛擬游標層。
- 本機 `http://localhost:3000/` 可正常載入，瀏覽器開發記錄沒有新的 error／warning。
- 尚未做從正式 `chapter03-section-9` 完整遊玩到兩條存檔分支的人工 E2E；目前沒有獨立的 `chapter03-End` Debug 入口，本次沒有為了測試而改寫玩家正式進度。

## 6. 既有未通過測試

完整測試仍有以下 8 個既有資料／斷言落差，與本次章末存檔 checkpoint 無直接關係：

1. BGM 測試仍期待舊的 `MAIN_001`／`MAIN_002` 音量規則。
2. 任務提示音效在 `Assets/Audio` 與 `public/audio` 的內容不一致。
3. 發電機末段 FadeOut 測試期待 `15%`，目前設定為 `30%`。
4. 焊接成功音量測試期待 `1`，目前設定為 `0.4`。
5. 背包教學任務的 OBJ 數量測試期待 `3`，目前資料為 `2`。
6. `MAIN_003` COMPLETE 後延遲測試期待 `1 秒`，目前資料為 `0.5 秒`。
7. `MAIN_005` 隱藏工具鏈測試仍期待舊 Stage 條件，目前資料引用 `MAIN_004_STAGE_01`。
8. `chapter03-section-9` 複製測試要求 Line ID 與 `chapter03-final` 完全相同，但目前新規格會為複製段落產生獨立 Line ID。

## 7. 後續接手注意事項

- 第四章正式任務、腳本、互動與存檔欄位一律沿用 `chapter04` 命名。
- 不要在存檔成功前先改 runtime 的 `currentChapter`；否則寫檔失敗時會留下「畫面仍在章末、狀態已進第四章」的不一致。
- 若日後其他章節也要相同存檔關卡，沿用 ChapterFlow 的 `beforeFadeOutCheckpointId`，不要複製另一套黑幕計時器。
- 若新增章末 Debug 測試入口，必須隔離正式 SaveData，避免測試直接覆寫玩家正常存檔。
