# Echoes 專案每日摘要 — 2026-07-30

> 時區：Asia/Taipei  
> 專案：`D:\Codex\Echoes`  
> 整理時間：2026-07-30 00:00 後  
> 比對基準：`D:\Codex\Echoes\摘要.md`（2026-07-29 09:55）。`docs/daily-summaries` 原先沒有日期摘要，因此本次以根目錄摘要與 Git 紀錄交叉比對。

## 完成事項

### MapEditor 調整與路徑整理

- 2026-07-29 18:32 已建立提交 `d5d3827`（`調整地圖編輯器`）。
- MapEditor 原始碼由 `tools/MapEditor/` 移至專案根目錄的 `MapEditor/`。
- 可執行檔由根目錄 `MapEditor.exe` 改放至 `MapEditor/MapEditor.exe`。
- 同步更新 `.gitignore`、`README.md`、`MapEditor/README.md`、`docs/scene-format.md` 與 `public/maps/map_test01.scene.json`。
- `map_test01.scene.json` 在該提交中有較大幅度的場景資料調整；目前工作樹又有數個 Collision／多邊形節點座標的小幅修正，尚未提交。

### 手把操作 DEBUG 選單

- 尚未提交的 `app/movement-lab.tsx` 與 `app/globals.css` 已加入：
  - START 開啟／關閉 DEBUG 選單。
  - B 在 DEBUG 選單開啟時關閉選單。
  - 十字鍵上下循環選取項目。
  - 十字鍵左右調整拉桿。
  - A 確認切換項目。
  - DEBUG 選單開啟期間，左搖桿仍直接控制角色移動。
  - X 保留為手把游標點擊／互動動作。
  - 目前選取的按鈕或拉桿會以外框、底色與左側色條標示。
- DEBUG 選單可控制的項目順序：玩家碰撞、場景碰撞、BGM 開關、BGM 音量、移動速度、角色尺寸、碰撞滑動寬容度。

### 角色行走參考與預覽素材

- 新增未追蹤的 `public/characters/walk/Walking_se_01.png` 至 `Walking_se_16.png`，共 16 張參考行走幀。
- `public/characters/walk/04_SE_FrontRight/pose-transfer-preview-01-08/` 目前有 15 張姿勢轉換預覽圖。
- `public/characters/walk/04_SE_FrontRight/` 目前有兩張未追蹤的主角行走候選圖：
  - `04_SE_FrontRight_MixamoWalk_01.png`
  - `04_SE_FrontRight_MixamoWalk_02.png`
- 已改良姿勢比對流程：先產生候選圖，再用原始參考角色與候選角色做重疊預覽，經人工確認後才可列為正式遊戲素材。
- 重要修正：參考圖必須保留原始顏色、明暗、肢體前後遮擋與左右腳細節；不可先轉成單色剪影，否則容易把角色本人的左／右腳判反。
- 現行預覽規格：參考圖保持原色 100%，候選角色約 42% 透明度，另以橘色外框顯示候選輪廓。預覽檔放在忽略追蹤的 `work/pose_previews/`。

## 功能或技能

- XInput 與瀏覽器 Gamepad API 的按鍵資料已擴充 START、B、A、X、十字鍵與左右搖桿的獨立狀態。
- DEBUG 選單操作已改用獨立選取狀態與可重複輸入的十字鍵導覽。
- 建立透明角色圖片時使用純色去背流程，並驗證輸出 PNG 的 Alpha 透明角落。
- 姿勢審查改採「原始參考／候選角色／重疊結果」三欄比較，避免只看單張生成圖誤判步態。

## 重要參數與設計決策

- DEBUG 十字鍵首次連續輸入延遲：`0.32` 秒。
- DEBUG 十字鍵連續輸入間隔：`0.12` 秒。
- BGM 音量每次增減：5%。
- 移動速度每次增減：10，允許範圍 100–380。
- 角色尺寸每次增減：4，允許範圍 90–220。
- Collision 滑動寬容度每次增減：5%，允許範圍 20–100%。
- DEBUG 選單開啟時，十字鍵只操作選單；角色移動改為只讀取左搖桿，避免十字鍵同時移動角色。
- 角色姿勢的左右一律以角色本人的解剖學左／右判定，不以畫面左／右代替。
- 行走姿勢未經重疊預覽確認，不得直接放入正式角色動畫。

## 測試結果

- `pnpm run lint`：第一次因自動化環境找不到 Node.js 而失敗；改用 Codex 內建 Node.js 後完成，0 errors、772 warnings。警告全部來自產生後的 `pages-dist/assets/*.js`，目前 lint 指令未忽略 `pages-dist`。
- `pnpm run build`：成功，Vinext 五階段建置完成。
- `pnpm run build:pages`：成功；輸出 `pages-dist/index.html`、CSS 約 5.54 kB、JS 約 225.23 kB。
- `dotnet build MapEditor/MapEditor.csproj -c Release`：成功，0 warnings、0 errors。
- `node --test tests/rendered-html.test.mjs`：2 項失敗。
  - 測試仍期待 `<meta name="codex-preview" content="development">`，目前輸出的 HTML 沒有此標記。
  - 測試仍讀取已不存在的 `app/_sites-preview/SkeletonPreview.tsx`。
- 結論：實際前端與 MapEditor 均可完成建置；HTML 測試與目前專案架構不同步，需要後續更新。

## 已知問題

- `Walking_se_02` 的正確動作是角色本人的左腳邁出；先前生成候選曾把左右腳或前後遮擋判反，目前仍在預覽確認階段，不能視為已完成動畫素材。
- `04_SE_FrontRight_MixamoWalk_02.png` 已存在於未追蹤的遊戲素材路徑，但姿勢尚未正式核准，不應接入動畫播放。
- 單靠外輪廓無法準確比較穿戴背包與護具的主角和纖細參考模型；必須同時查看原始參考圖的肢體細節與遮擋關係。
- 根目錄舊 `摘要.md` 在目前 PowerShell 顯示為亂碼；本日期摘要以 UTF-8 建立。
- lint 會掃描 `pages-dist` 的壓縮輸出，造成大量無實質程式碼價值的警告。
- `tests/rendered-html.test.mjs` 仍依賴舊的 Codex 預覽骨架與 Meta 設計。

## 注意事項

- 不要把 `work/pose_previews/` 的比較圖當成遊戲素材。
- 後續產生行走幀時，必須先確認：角色本人左／右腳、支撐腳、邁步腳、膝蓋交錯、腳尖位置及肢體前後遮擋。
- MapEditor 現在的正式專案路徑是 `MapEditor/`，不是已移除的 `tools/MapEditor/`。
- 工作樹仍含使用者未提交修改；不可用 reset、checkout 或清理指令覆蓋。
- 本次自動摘要只新增本文件，未修改遊戲程式，未自動 Commit 或 Push。

## Git 提交與 Push 狀態

- 分支：`main`。
- 遠端：`origin = https://github.com/sbaak85/Echoes`。
- 本機 `main` 與本機記錄的 `origin/main`：ahead 0、behind 0。
- 最新提交：`d5d382780000c07ae1bbff91334c9253908f8176`，`調整地圖編輯器`，2026-07-29 18:32:45 +0800。
- 因 `main` 與 `origin/main` 相同，最新提交已在目前遠端追蹤狀態中；本次沒有執行網路 Push。
- 尚未提交的追蹤檔：
  - `app/globals.css`
  - `app/movement-lab.tsx`
  - `public/maps/map_test01.scene.json`
- 尚未追蹤：`public/characters/walk/`、根目錄 `摘要.md`，以及本次新增的 `docs/daily-summaries/2026-07-30-摘要.md`。

## 下一步

1. 使用完整原色的 `Walking_se_02` 重疊預覽，先確認角色本人左腳邁出與前後腿遮擋是否正確。
2. 只有在姿勢確認後，才把核准候選另存為正式遊戲動畫幀。
3. 確認 16 幀參考圖、15 張舊姿勢預覽及兩張 MixamoWalk 候選哪些需要保留並納入 Git。
4. 更新 `tests/rendered-html.test.mjs`，移除對舊 `_sites-preview` 與 `codex-preview` Meta 的依賴，或恢復相應結構。
5. 將 `pages-dist` 加入 lint 忽略範圍，避免壓縮產物造成大量警告。
6. 由使用者確認工作樹內容後，再另行 Commit／Push。
