# 2026-09-03 任務顯示進度預設值修正摘要

## 摘要定位

- 本機專案權威路徑：`C:\Users\sbaak.fang\ChatGPT\EchoesBeyond`。
- 本次調整 QuestEditor 新建 Objective 時，「顯示進度」布林值的預設狀態。
- 功能原始碼與執行檔已包含在 Git commit `03371b8`；本檔補記功能規格、驗證結果與後續注意事項。
- 本次涉及 QuestEditor 屬性面板；專案的瀏覽器遊戲 UI 後續仍須遵守 [瀏覽器 UI 與手把操作規範](../../.codex/skills/browser-ui-focus-visuals/SKILL.md)。本次未修改瀏覽器遊戲 UI，沒有刻意例外。

## 1. 功能調整

- `QuestObjectiveDefinition.ShowProgress` 的預設值由 `true` 改為 `false`。
- 使用 QuestEditor 新增 Objective 時，屬性設定中的「顯示進度」現在預設顯示為 `False`。
- 本次只影響之後新建的 Objective；既有任務資料中已保存的 `showProgress: true/false` 不會被批次覆寫或遷移。
- 使用者仍可依個別 Objective 的需求，手動將「顯示進度」改為 `True`。

## 2. 回歸保護

- QuestEditor self-test 新增預設值檢查。
- 如果日後 `ShowProgress` 的新建預設值被改回 `true`，self-test 會以錯誤中止，避免變更未被發現。

## 3. 修改檔案

- `QuestEditor/QuestModels.cs`
- `QuestEditor/Program.cs`
- `QuestEditor/QuestEditor.exe`

## 4. 建置、測試與部署

- `dotnet build QuestEditor/QuestEditor.csproj -c Release`：成功，`0` error。
- 建置出現 `3` 個 `NETSDK1138` 警告，原因是既有目標框架 `net6.0-windows` 已結束支援；與本次功能修改無關。
- 部署後的 `QuestEditor/QuestEditor.exe --self-test`：通過，Exit Code `0`。
- 部署後的 `QuestEditor/QuestEditor.exe --ui-smoke-test`：通過，Exit Code `0`。
- 已重新發布並更新專案實際使用的 `QuestEditor/QuestEditor.exe`。
- 發布檔與部署檔 SHA-256 相符：`FD16280587177AA7CA46F5C1A130ED500E159A8A75D2E8D7197BE735956A200B`。

## 5. 後續注意事項

- 單純讀取舊任務資料時，必須保留 JSON 內既有的 `showProgress` 值，不應因新預設值改為 `false` 而覆寫舊內容。
- 只有需要顯示數量、比例或累積進度的 Objective，才建議手動開啟「顯示進度」。
